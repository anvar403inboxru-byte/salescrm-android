name: orbitson_crm
description: ORBITSON CRM — Satış idarəetmə sistemi
publish_to: 'none'
version: 1.6.0+7

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.3.1
  provider: ^6.1.2
  go_router: ^14.2.7
  intl: ^0.19.0
  flutter_secure_storage: ^9.2.2
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  url_launcher: ^6.3.0
  file_picker: ^8.1.2
  open_filex: ^4.5.0
  path_provider: ^2.1.4
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/